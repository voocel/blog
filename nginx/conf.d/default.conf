# Default HTTP configuration (before HTTPS setup)
server {
    listen 80;
    server_name _;

    # ACME verification path (required for SSL certificate)
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
    }

    # Frontend (static files)
    location / {
        proxy_pass http://frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Backend API
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeout settings
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Backend static files
    location /static/ {
        proxy_pass http://backend:8080/static/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;

        # Cache static files
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS configuration (will be enabled after running setup-https.sh)
# Uncomment the block below after SSL certificate is generated
#
# server {
#     listen 443 ssl http2;
#     server_name _;  # Will be replaced with actual domain
#
#     # SSL certificate
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # Security headers
#     add_header Strict-Transport-Security "max-age=31536000" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#
#     # Frontend (static files)
#     location / {
#         proxy_pass http://frontend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     # Backend API
#     location /api/ {
#         proxy_pass http://backend:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         # Timeout settings
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#
#     # Backend static files
#     location /static/ {
#         proxy_pass http://backend:8080/static/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#
#         # Cache static files
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
# }
#
# # Redirect HTTP to HTTPS (enable after HTTPS is configured)
# server {
#     listen 80;
#     server_name _;  # Will be replaced with actual domain
#
#     # ACME verification path
#     location ^~ /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#         default_type "text/plain";
#     }
#
#     # Redirect all other requests to HTTPS
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
